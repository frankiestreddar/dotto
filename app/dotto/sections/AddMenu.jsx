"use client";

import MarkupSection from "./MarkupSection";

// Shell markup for "add-menu", migrated verbatim from Dotto.html.
// See content/fragments/add-menu.html for the source HTML.
export default function AddMenu({ html }) {
  return <MarkupSection html={html} />;
}
