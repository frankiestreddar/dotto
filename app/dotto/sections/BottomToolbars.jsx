"use client";

import MarkupSection from "./MarkupSection";

// Shell markup for "bottom-toolbars", migrated verbatim from Dotto.html.
// See content/fragments/bottom-toolbars.html for the source HTML.
export default function BottomToolbars({ html }) {
  return <MarkupSection html={html} />;
}
