"use client";

import MarkupSection from "./MarkupSection";

// Shell markup for "canvas-area", migrated verbatim from Dotto.html.
// See content/fragments/canvas-area.html for the source HTML.
export default function CanvasArea({ html }) {
  return <MarkupSection html={html} />;
}
