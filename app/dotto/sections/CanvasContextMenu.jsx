"use client";

import MarkupSection from "./MarkupSection";

// Shell markup for "canvas-context-menu", migrated verbatim from Dotto.html.
// See content/fragments/canvas-context-menu.html for the source HTML.
export default function CanvasContextMenu({ html }) {
  return <MarkupSection html={html} />;
}
