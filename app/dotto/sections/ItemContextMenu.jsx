"use client";

import MarkupSection from "./MarkupSection";

// Shell markup for "context-menu", migrated verbatim from Dotto.html.
// See content/fragments/context-menu.html for the source HTML.
export default function ItemContextMenu({ html }) {
  return <MarkupSection html={html} />;
}
