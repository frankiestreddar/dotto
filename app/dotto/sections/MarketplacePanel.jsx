"use client";

import MarkupSection from "./MarkupSection";

// Shell markup for "cart-panel", migrated verbatim from Dotto.html.
// See content/fragments/cart-panel.html for the source HTML.
export default function MarketplacePanel({ html }) {
  return <MarkupSection html={html} />;
}
