"use client";

import MarkupSection from "./MarkupSection";

// Shell markup for "profile-panel", migrated verbatim from Dotto.html.
// See content/fragments/profile-panel.html for the source HTML.
export default function ProfilePanel({ html }) {
  return <MarkupSection html={html} />;
}
