"use client";

import MarkupSection from "./MarkupSection";

// Shell markup for "source-add-menu", migrated verbatim from Dotto.html.
// See content/fragments/source-add-menu.html for the source HTML.
export default function SourceAddMenu({ html }) {
  return <MarkupSection html={html} />;
}
