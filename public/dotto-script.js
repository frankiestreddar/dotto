// Phase 4.1's cluster revisit ported the last remaining files here (see PHASE4_ROADMAP.md) —
// public/dotto/ itself is now empty. This file is left in place, empty, as the entry point
// app/dotto-app.jsx's own <Script> tag still references; deleting it (and that tag, and
// bridges.js's createStore mechanism) is Phase 4.6's own closing task, not this one's.
